class BetterunModsApi {
    getModSettings(modId) {
        // window.addEventListener("message", (event) => {
        //     if (event.data.betterun) {
        //         console.log(event)
        //         window.postMessage({ betterunClient: "received" })
        //     }
        // });
        // TODO
    }
}

const betterun = new BetterunModsApi();